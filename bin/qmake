#!/bin/sh

# This script runs the qhost executable found in the PATH. You may safely
# replace this file with a copy/symlink of your choice.

qhost --config-file $(dirname $(realpath $0))/qhost.json $@
